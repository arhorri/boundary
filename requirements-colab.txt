# Remote-session dependencies for Colab / Kaggle.
# Mirrors environment.yml EXACTLY, minus pytorch + torchvision:
# both hosts ship those preinstalled and reinstalling them breaks the
# CUDA build that matches the host driver.
#
# A host torch that is too old must fail loudly. There is no way to assert
# a version inside a requirements file, so the assert lives in
# scripts/bootstrap_session.py (TORCH_MIN / TORCHVISION_MIN) and runs before
# anything else. Keep these two numbers in sync with environment.yml:
#   torch        >= 2.1.2
#   torchvision  >= 0.16.2

numpy==1.26.4
scipy==1.11.4
scikit-image==0.22.0
pyyaml==6.0.1
tqdm==4.66.1
matplotlib==3.8.2
pandas==2.1.4
tensorboard==2.15.1
opencv-python==4.9.0.80
albumentations==1.3.1
segmentation-models-pytorch==0.3.3
